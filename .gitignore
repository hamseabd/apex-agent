# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
dist/
build/

# Env & secrets
.env
.env.*
*.pem
*.key

# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/*.tfplan
terraform/*.tfvars

# AWS
.aws/

# macOS
.DS_Store

# IDE
.vscode/
.idea/
*.swp

# Lambda artifacts
*.zip

# Internal docs — never committed
.notes/
docs/

# Claude Code local memory — never committed
.claude/

# User protocol — generated via /setup, lives on S3
apex.yaml

# Eval run outputs — JSONL is per-run noise; HISTORY.md is committed
evals/results/*.jsonl

# Coverage artifacts
.coverage
htmlcov/
